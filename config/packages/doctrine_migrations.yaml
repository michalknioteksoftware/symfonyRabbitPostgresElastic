doctrine_migrations:
    migrations_paths:
        'App\Migrations': '%kernel.project_dir%/migrations'
    # Disable the profiler collector — it runs a live DB query on every
    # profiled request to compare applied vs available migrations, which
    # makes every page slow in dev until all migrations are applied.
    enable_profiler: false
