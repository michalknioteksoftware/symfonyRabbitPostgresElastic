when@dev:
    web_profiler:
        toolbar: true
        intercept_redirects: false

    framework:
        profiler:
            only_exceptions: false
            # Explicit file DSN so the storage location is never ambiguous
            dsn: 'file:%kernel.cache_dir%/profiler'
