when@dev:
    web_profiler:
        toolbar: true
        intercept_redirects: false

    framework:
        profiler:
            only_exceptions: false
            # Store profiler data outside var/cache/dev so cache:clear never
            # wipes it — tokens stay valid across cache rebuilds.
            dsn: 'file:%kernel.project_dir%/var/profiler'
